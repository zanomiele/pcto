#include <stdio.h>
#include <math.h>
int main() 
{
   int a;
   int b;
   int c4;
   int x1;
   int x2;
   int delta;

   printf("inserisci 5 numeri\n");
   scanf("%d %d %d %d", &a, &b, &c4);
delta=b*b-4*a*c4;
if(delta<0)
{
    printf("l'equazione è impossibile");


}

{

    if else(delta>0)
    {
        x1= (-b+sqrt(delta))/(2*a);
        x2= (-b-sqrt(delta))/(2*a);
    }
}}